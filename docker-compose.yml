version: '3'
services:
    frontend:
        build: hc-demo-frontend
        ports:
            - "8080:80"
        links:
            - backend
    backend:
        build: hc-demo-backend
        links:
            - kafka
    generator:
        build: hc-demo-generator
        links:
            - kafka
    kafka:
        image: wurstmeister/kafka
